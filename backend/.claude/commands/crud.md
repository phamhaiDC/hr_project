# crud

Generate full CRUD module for a NestJS application.

Requirements:

- Create:
  - Controller
  - Service
  - DTOs (Create + Update)
- Use Prisma for database
- Follow clean architecture

Include:

- Create API
- Get list API (with pagination)
- Get detail API
- Update API
- Delete API

Code must be production-ready.